/**
 * Eval
 * 
 * Slow
 * Insecure
 * Unnecessary
 * Use only if you don't ever want to be hired
 * 
 */

debugger;

eval("alert('do not hire me')");


/**
 * JSON
 * 
 * uses object litrials
 * replacement for XML
 * Humans can understand this
 * Less verbose (smaller over wire)
 * You can use directly
 */

debugger;

var json = {
    name: "Sachin",
    sports:[
        "Snowboarding",
        "Soccer",
        "Football"
    ],
    worksAtMicrosoft: true
}

console.log(JSON.stringify(json));

debugger;

/**
 * isNaN & parse
 */
var r=isNaN(NaN);
r=isNaN(1);
r=isNaN("A");
r=isNaN("1");
r = parseFloat("4.32");
r = parseInt("4.21");  

debugger;

/**
 * Math
 * 
 * abs
 * ceil
 * floor
 * random
 * round
 */
