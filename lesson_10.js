/**
 * String
 * 
 * Primitive type (orederd set of characters)
 * Created using one of two litrial notations
 * 
 */

debugger;

var s1 = "I am one string";
var s2 = 'Some believe Drumpf will make America "Great" again';
var escape = 'There is no escaping Felix\'s wrath';
var copyright = 'copyright \u00A9';


var join = "I " + "am " + " a string";

var x = join.charAt(0);

var i = join.indexOf('a');

var re = join.replace("string","human");

var found = join.search("string");

var slice = "slice".slice(3);

var split = "1,2,3,4,5".split(',');

var lower = "UPPERCASE".toLowerCase();

var upper = lower.toUpperCase();
